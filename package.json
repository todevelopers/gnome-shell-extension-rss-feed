{
  "name": "gnome-shell-extension-rss-feed",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "shexli": "node scripts/shexli.js",
    "check": "npm run lint && npm run shexli"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "vitest": "^3.0.0"
  }
}
